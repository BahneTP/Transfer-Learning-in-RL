_target_: src.algorithms.atari100k.SPRAtari100KAlgorithm

obs_key: pixels
replay_capacity: 200_000
observation_shape: [4, 84, 84]

gamma: 0.99
update_horizon: 10
min_replay_history: 2000
target_update_period: 1
epsilon_train: 0.0
epsilon_eval: 0.001
epsilon_decay_period: 2001
replay_ratio: 64
batch_size: 32
learning_rate: 0.0001
adam_eps: 0.00015
weight_decay: 0.0
max_grad_norm: 10.0
spr_weight: 5.0
jumps: 5

frames_per_batch: 1
max_frames_per_traj: -1
seed: ${trainer.seed}
